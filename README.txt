remove_column.bas is a VBA script.
It copies the active sheet, then removes a specified column from the copy.
There are a couple of user configuration options optional to edit inside the script as well.


To set up the Excel macro, follow the Instructions and/or consult Internet.
